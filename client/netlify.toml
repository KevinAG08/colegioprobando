# Configuración de Build (opcional si ya está en la UI de Netlify, pero bueno tenerla)
[build]
  command = "npm run build" # Tu comando de build de client/package.json
  publish = "dist"          # Tu directorio de publicación relativo a 'client'

# Regla de redirección para SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
