# my-fullstack-app/.gitignore

# Dependencias de Node (general)
node_modules/ # Si tuvieras un node_modules en la raíz principal

# Archivos de Node.js API (anteriormente server)
api/node_modules/
api/dist/         # Carpeta de compilación de TypeScript
api/.env          # Variables de entorno
api/generated/    # Archivos generados por Prisma (como el cliente)
api/prisma/migrations/ # Migraciones de Prisma (opcional, pero buena práctica si las regeneras)

# Archivos de Cliente React/Vite
client/node_modules/
client/dist/      # Carpeta de build de Vite

# Archivos de configuración y logs comunes
.env              # Si tuvieras un .env en la raíz
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel/          # Carpeta de configuración local de Vercel